services:
  - type: web
    name: invoice-qc-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn invoice_qc.api.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0